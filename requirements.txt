# Streamlit Cloud deployment requirements.
# Excludes langchain-openai (pulls in tiktoken which requires a Rust compiler)
# and langchain-google-genai / langchain-experimental (not used by this app).
# Streamlit Cloud reads this file instead of pyproject.toml.

streamlit>=1.28.0
python-dotenv

# LLM — Anthropic only
langchain-anthropic>=0.3.15
langchain-core>=0.3.81
langgraph>=0.4.8

# Data
yfinance>=0.2.63
pandas>=2.3.0
stockstats>=0.6.5
backtrader>=1.9.78.123

# Utilities
requests>=2.32.4
pytz>=2025.2
rank-bm25>=0.2.2
redis>=6.2.0
rich>=14.0.0
tqdm>=4.67.1
typer>=0.21.0
questionary>=2.1.0
parsel>=1.10.0
typing-extensions>=4.14.0
setuptools>=80.9.0
