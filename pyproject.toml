[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tradingagents-guide"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
